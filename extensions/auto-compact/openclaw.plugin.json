{
  "id": "auto-compact",
  "name": "Auto-Compaction",
  "description": "Automatic micro-compaction to prevent token limit overruns",
  "version": "1.0.0",
  "kind": "lifecycle",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable automatic compaction"
      },
      "triggerPercentage": {
        "type": "number",
        "default": 90,
        "minimum": 50,
        "maximum": 98,
        "description": "Trigger compaction at this percentage of token budget"
      },
      "minTokensToKeep": {
        "type": "number",
        "default": 20000,
        "minimum": 1000,
        "description": "Minimum tokens to preserve after compaction"
      },
      "maxTokensToKeep": {
        "type": "number",
        "default": 50000,
        "minimum": 1000,
        "description": "Maximum tokens to preserve after compaction"
      },
      "targetAfterCompact": {
        "type": "number",
        "default": 30000,
        "minimum": 1000,
        "description": "Target token count after compaction"
      },
      "preserveRecentTurns": {
        "type": "number",
        "default": 10,
        "minimum": 1,
        "description": "Preserve this many recent turns verbatim"
      },
      "notifyUser": {
        "type": "boolean",
        "default": true,
        "description": "Send notification when compaction occurs"
      },
      "summaryModel": {
        "type": "string",
        "description": "Optional model override for summarization (provider/model)"
      }
    }
  },
  "uiHints": {
    "enabled": {
      "label": "Enable Auto-Compaction",
      "description": "Automatically compact sessions when approaching token limit"
    },
    "triggerPercentage": {
      "label": "Trigger Threshold (%)",
      "description": "Compact when token usage reaches this percentage"
    },
    "minTokensToKeep": {
      "label": "Minimum Tokens to Keep",
      "description": "Don't compact if it would leave less than this"
    },
    "maxTokensToKeep": {
      "label": "Maximum Tokens to Keep",
      "description": "Never keep more than this after compaction"
    },
    "targetAfterCompact": {
      "label": "Target After Compaction",
      "description": "Ideal token count after compaction"
    },
    "preserveRecentTurns": {
      "label": "Recent Turns to Preserve",
      "description": "Always keep this many recent turns intact"
    },
    "notifyUser": {
      "label": "Notify on Compaction",
      "description": "Send message to user when compaction occurs"
    },
    "summaryModel": {
      "label": "Summary Model Override",
      "description": "Use a different model for generating summaries (optional)"
    }
  }
}
