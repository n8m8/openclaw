{
  "id": "auto-compact",
  "name": "Auto-Compaction",
  "description": "Automatic micro-compaction to prevent token limit overruns",
  "version": "2.0.0",
  "kind": "lifecycle",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true,
        "description": "Enable automatic micro-compaction"
      },
      "triggerPercentage": {
        "type": "number",
        "default": 90,
        "minimum": 50,
        "maximum": 98,
        "description": "Trigger micro-compaction at this percentage of token budget"
      },
      "preserveRecentResults": {
        "type": "number",
        "default": 5,
        "minimum": 0,
        "maximum": 50,
        "description": "Preserve this many recent tool results (don't clear)"
      },
      "notifyUser": {
        "type": "boolean",
        "default": true,
        "description": "Send notification when compaction occurs"
      },
      "compactableTools": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Tool names to compact (empty = use defaults)"
      }
    }
  },
  "uiHints": {
    "enabled": {
      "label": "Enable Micro-Compaction",
      "description": "Automatically clear verbose tool outputs when approaching token limit"
    },
    "triggerPercentage": {
      "label": "Trigger Threshold (%)",
      "description": "Clear tool outputs when token usage reaches this percentage"
    },
    "preserveRecentResults": {
      "label": "Recent Results to Preserve",
      "description": "Don't clear the last N tool outputs (keeps recent context intact)"
    },
    "notifyUser": {
      "label": "Notify on Compaction",
      "description": "Send message when tool outputs are cleared"
    },
    "compactableTools": {
      "label": "Compactable Tool Names",
      "description": "Which tools to clear (leave empty for defaults: Read, exec, grep, glob, web_search, web_fetch, Edit, Write)",
      "advanced": true
    }
  }
}
